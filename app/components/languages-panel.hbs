<ProfilePanel @title="Languages" @icon="🗣">
    <div class="space-y-4">
        {{#each @languages as |language|}}
        <div class="group hover:bg-gray-50 p-2 rounded-md transition-colors duration-200">
            <div class="flex justify-between items-center">
                <span class="font-medium text-gray-900">{{language.name}}</span>
                <span class="text-sm text-gray-500">{{language.proficiency}}</span>
            </div>
            <div class="mt-2 bg-gray-200 rounded-full h-2">
                <div class="bg-blue-500 h-2 rounded-full transition-all duration-500 ease-out"
                    style="width: {{language.level}}%;"></div>
            </div>
        </div>
        {{/each }}
    </div>
</ProfilePanel>